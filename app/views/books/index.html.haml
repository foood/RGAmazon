%h1 Books#index
%table.table.table-striped
  %thead
    %th Title
    %th Author
    %th Price
    %th
  %tbody
    - @books.each do |book|
      %tr
        %td= book.title
        %td= book.author
        %td= number_to_currency(book.price)
        %td= link_to 'View Details', book, remote: true
%br/
= link_to raw("<i class=icon-plus></i> New Book"), new_book_path, class: "btn"