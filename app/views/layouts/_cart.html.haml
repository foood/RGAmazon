%li.nav.nav-pills{:role => "tablist"}
  %li.active{:role => "navigation"}
    %a.cart{:href => cart_path}
      %span.glyphicon.glyphicon-shopping-cart{"aria-hidden" => "true"}

      %span.badge #{current_order.order_items.size} total
      %span.badge  #{number_to_currency current_order.subtotal}
